document.getElementById('submit').onclick = function() {
      const commentView = document.getElementsByClassName('comment');
      const commentNum = commentView.length;
      console.log(commentNum );

      // const commentCount = document.getElementById('comment-count');
      // commentCount.innerHTML = `${commentNum}件` 
    }
  
